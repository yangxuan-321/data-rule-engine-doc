需求风暴 -> 
```
    ## 规则引擎
    ## 规则引擎抽象层
    ## 事件管理器
    ## 组件抽象层
```

```
    1. 任务 是否 支持回退 - 回退到 上一个节点 【流程引擎】
    2. 我们在处理数据任务的时候 - 如果是分布式的，我们的数据 处理 超时，或者 处理的任务 挂了，那么 这个 任务 已经被它获取了，它如何释放掉 
        那么 2 的 计算节点 因为 一直都没有 释放 那个节点的任务,所以 别人 也会取 不到，即便 只是 挂了一台 机器。那么 其他机器 也能计算 但是获取 不到任务
        所以 我们 再设计的时候，一定要保证 系统的 容错性和可恢复性【流处理平台的心跳保持？】
```